# Form backend for the contact section (src/components/Contact.jsx).
# Default: /api/contact — the Cloudflare Pages Function (functions/api/contact.js + RESEND_API_KEY
# secret on the Pages project). Point it at a hosted backend (Formspree/Web3Forms) to override.
VITE_FORM_ENDPOINT=/api/contact
